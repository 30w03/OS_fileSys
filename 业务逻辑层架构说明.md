# 业务逻辑层架构说明

业务逻辑层（Business Logic Layer）是系统的核心，负责处理具体的业务规则、状态流转和数据处理。它起到了承上启下的作用：向上响应网络层的请求，向下调用文件系统层进行数据持久化。

## 1. 核心模块划分

业务逻辑层采用模块化设计，主要由以下几个核心管理器（Manager）组成，每个管理器负责一类特定的业务实体：

### 1.1 UserManager（用户管理器）
- **职责**：管理用户的注册、登录、权限验证。
- **核心逻辑**：
  - **身份验证**：验证用户名和密码的正确性。
  - **权限控制**：检查用户是否具有执行某项操作的权限（如 ACL 检查）。
  - **状态维护**：维护用户的在线状态和会话信息。
- **数据交互**：调用 `Filesystem` 读取和保存用户信息文件（如 `/users/user_data.db`）。

### 1.2 PaperManager（论文管理器）
- **职责**：管理论文的提交、查询、状态更新。
- **核心逻辑**：
  - **ID 生成**：为新提交的论文生成全局唯一的 ID。
  - **完整性检查**：验证论文格式、标题、作者等信息是否完整。
  - **生命周期管理**：维护论文的状态流转（已提交 -> 评审中 -> 已录用/已拒绝）。
- **数据交互**：调用 `Filesystem` 将论文元数据和内容写入虚拟磁盘。

### 1.3 ReviewManager（评审管理器）
- **职责**：管理评审意见的提交、分配、查询。
- **核心逻辑**：
  - **评审分配**：将论文分配给合适的评审人（Reviewer）。
  - **意见收集**：收集评审人的评分（Score）和评论（Comments）。
  - **决策支持**：根据评审结果计算论文的最终得分，辅助录用决策。
- **数据交互**：调用 `Filesystem` 存储评审记录，并关联 Paper 和 User。

---

## 2. 业务逻辑层的协作流程

业务逻辑层不直接操作底层资源，而是通过清晰的接口调用来实现功能。

### 2.1 向上对接网络层
- **请求处理**：网络层（Server）解析出具体的请求消息（如 `MSG_LOGIN_REQUEST`）后，直接调用业务逻辑层的对应接口（如 `UserManager::login`）。
- **结果返回**：业务逻辑层处理完毕后，返回业务对象（如 `LoginResult`），网络层再将其封装为响应消息发送回客户端。

### 2.2 向下调用文件系统层
- **接口调用**：业务逻辑层只调用文件系统的高级接口（如 `createFile`, `writeFile`, `readFile`），不关心底层的块分配和 Inode 管理。
- **序列化**：业务对象（如 `User` 结构体）在写入文件系统前，由业务逻辑层负责序列化为字节流；读取时再反序列化为对象。

---

## 3. 示例流程：提交评审 (Submit Review)

以下是一个“提交评审”操作在业务逻辑层内部的完整处理流程：

**1. 接收请求**
- `Server` 收到 `MSG_SUBMIT_REVIEW_REQUEST` 消息。
- 解析出参数：`paperId`（论文ID）, `reviewerId`（评审人ID）, `score`（评分）, `comment`（评论）。
- 调用 `ReviewManager::submitReview(paperId, reviewerId, score, comment)`。

**2. 业务校验 (Validation)**
- **存在性检查**：调用 `PaperManager` 确认 `paperId` 对应的论文是否存在。
- **权限检查**：调用 `UserManager` 确认 `reviewerId` 是否有权限评审该论文（例如，不能评审自己的论文）。
- **重复性检查**：检查该评审人是否已经评审过该论文，避免重复提交。

**3. 状态更新 (State Transition)**
- 创建一个新的 `Review` 对象，填充评审信息和时间戳。
- 更新论文的状态（例如，如果一篇论文收集齐了 3 个评审，状态可能自动变更为“评审完成”）。

**4. 数据持久化 (Persistence)**
- 将 `Review` 对象序列化为字节流。
- 调用 `Filesystem::writeFile("/reviews/" + reviewId, data)` 将数据保存到虚拟磁盘。

**5. 返回结果**
- 返回操作成功或失败的状态码给网络层，由网络层封装为响应消息。

---

## 总结

业务逻辑层的设计遵循**高内聚、低耦合**的原则：
- **模块化**：通过不同的 Manager 管理不同的实体，逻辑清晰。
- **解耦**：业务逻辑不依赖具体的网络协议（TCP/UDP）或存储介质（物理盘/虚拟盘），只关注业务规则本身。
- **可测试性**：由于不依赖底层实现，业务逻辑层可以很容易地进行单元测试。
