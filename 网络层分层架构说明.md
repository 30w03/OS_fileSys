# 网络层分层架构说明

网络层采用了分层设计，从底层的连接管理到高层的业务逻辑，各层职责清晰，相互协作。

## 1. 底层传输层（Transport Layer）

**核心组件**：
- `Connection`（连接管理器）
- `Socket`（套接字封装，隐含在 Connection 中）

**职责**：
- **连接管理**：封装底层的 TCP Socket，管理连接的建立与断开。
- **数据传输**：提供最基础的字节流发送和接收功能，屏蔽底层网络 IO 的细节（如 `send`/`recv` 系统调用）。
- **字节序处理**：处理网络字节序（Big Endian）与主机字节序的转换。
- **完整性保证**：确保数据的完整发送和接收（处理 TCP 粘包/拆包问题）。

**抽象程度**：
- 最底层，直接与操作系统网络接口交互。

**示例功能**：
- `sendMessage`：发送封装好的消息对象。
- `receiveMessage`：从网络流中读取并解析出消息对象。
- `sendAll`/`receiveAll`：循环调用系统接口确保数据完全传输。

---

## 2. 协议解析层（Protocol Layer）

**核心组件**：
- `Protocol`（协议处理器）
- `Message`（消息结构体）
- `MessageHeader`（消息头）

**职责**：
- **消息定义**：定义通信协议的格式（Header + Payload），包括魔数、版本、类型、长度、校验和等。
- **序列化/反序列化**：将业务对象（如 `LoginRequest`）转换为网络传输的字节流，反之亦然。
- **校验机制**：计算和验证 CRC32 校验和，确保数据在传输过程中未被篡改。
- **消息工厂**：提供便捷的接口创建各种类型的请求和响应消息。

**抽象程度**：
- 中间层，负责数据的格式化和解析，连接底层传输与高层业务。

**示例功能**：
- `createLoginRequest`：创建一个登录请求消息。
- `parseLoginResponse`：解析登录响应消息。
- `serialize`：将数据结构转换为字节流。
- `deserialize`：将字节流还原为数据结构。

---

## 3. 高层应用层（Application Layer）

**核心组件**：
- `Client`（客户端逻辑）
- `Server`（服务器逻辑）
- `Handler`（业务处理器，如 `handleLogin`, `handleSubmitPaper`）

**职责**：
- **Client**：
  - 面向用户的业务接口（如 `login`, `submitPaper`）。
  - 组装请求，调用底层发送，等待响应并返回结果。
- **Server**：
  - 监听端口，接受连接，维护客户端连接池。
  - 路由分发：根据消息类型（`MessageType`）将请求分发给对应的处理函数。
  - 业务逻辑执行：调用后端服务（如数据库、文件系统）处理请求，并生成响应。

**抽象程度**：
- 最顶层，直接面向用户业务需求，实现具体的应用逻辑。

**示例功能**：
- `Client::login`：用户登录操作，封装了发送请求和解析响应的全过程。
- `Server::run`：服务器主循环，持续监听并处理连接。
- `Server::handleClient`：处理单个客户端的所有请求交互。

---

## 层次关系总结

1.  **底层传输层**：
    - 负责“怎么传”，处理 Socket 和字节流。
    - 组件：`Connection`。

2.  **协议解析层**：
    - 负责“传什么”，定义数据格式和校验规则。
    - 组件：`Protocol`、`Message`。

3.  **高层应用层**：
    - 负责“为什么传”，实现具体的业务意图。
    - 组件：`Client`、`Server`。

---

## 示例流程：提交论文

以下是一个“提交论文”操作在这三个层次中的执行流程：

**1. 高层应用层（Client 端）**：
- 用户调用 `Client::submitPaper`。
- `Client` 收集论文信息（标题、内容等）。
- 调用 `Protocol::createSubmitPaperRequest` 创建消息对象。

**2. 协议解析层**：
- `Protocol` 将论文信息序列化为字节流。
- 添加消息头（类型为 `MSG_SUBMIT_PAPER_REQUEST`），计算 CRC32。
- 生成最终的 `Message` 对象。

**3. 底层传输层**：
- `Client` 调用 `Connection::sendMessage`。
- `Connection` 将 `Message` 对象转换为网络字节序的字节流。
- 通过 Socket 发送数据到服务器。

**(网络传输...)**

**4. 底层传输层（Server 端）**：
- `Server` 的 `Connection` 接收字节流。
- 还原为 `Message` 对象。

**5. 协议解析层**：
- `Protocol` 校验消息完整性（CRC32）。
- 解析消息头，识别类型为 `MSG_SUBMIT_PAPER_REQUEST`。

**6. 高层应用层（Server 端）**：
- `Server` 根据类型分发给 `handleSubmitPaper`。
- `handleSubmitPaper` 调用 `Protocol::parseSubmitPaperRequest` 获取论文数据。
- 执行保存论文的业务逻辑（写入文件系统）。
- 返回响应消息。
