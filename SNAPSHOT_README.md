# 文件快照 (Snapshot) 与写时复制 (CoW) 说明

## 1. 概述
为了实现高效的系统备份和版本回滚功能，我们在文件系统中实现了基于 **写时复制 (Copy-on-Write, CoW)** 技术的快照机制。

快照的核心原则是：**创建快照时并不立即复制所有数据，而是共享底层数据块。只有当数据被修改时，才为新数据分配空间并复制原有内容。** 这使得快照创建几乎是瞬间完成的，且极大节省了存储空间。

## 2. 核心数据结构变更

### 2.1 块管理器 (BlockManager)
**文件**: `include/filesystem/block_manager.h`

我们在块管理器中引入了引用计数机制，用于跟踪每个物理磁盘块被多少个 Inode (文件/目录) 所共享。

*   **`refCounts_`**: 内存中的映射表 (`std::map<uint32_t, uint32_t>`)，记录块号到引用计数的映射。
*   **`incRef(blockId)`**: 增加块的引用计数。
*   **`decRef(blockId)`**: 减少块的引用计数。当计数降为 0 时，物理块才会被真正释放。
*   **`isShared(blockId)`**: 检查块是否被多个所有者共享 (引用计数 > 1)。

### 2.2 快照管理器 (SnapshotManager)
**文件**: `include/filesystem/snapshot.h`

负责管理快照的生命周期。

*   **`snapshots_`**: 存储所有活动快照的列表。
*   **`createSnapshot()`**: 创建当前文件系统状态的快照。

## 3. 工作机制

### 3.1 快照创建流程 (Snapshot Creation)
当用户发起快照创建请求时：

1.  **复制根节点**: 系统复制当前的根目录 Inode (Root Inode)。
2.  **增加引用**: 遍历根目录引用的所有数据块，增加它们的引用计数。
3.  **保存状态**: 将复制出的 Inode ID 记录为快照点。此时，快照和当前文件系统完全共享数据。

### 3.2 写时复制逻辑 (CoW Logic)

当文件系统发生写入操作（修改文件或目录）时，CoW 机制会被触发以保护快照数据不被覆盖。

#### A. 文件写入 (File Write)
在 `FileOps::writeFile` 中：
1.  **检查共享**: 在写入数据块之前，检查目标块的引用计数。
2.  **复制块 (Copy)**: 如果块是共享的 (`refCount > 1`)，分配一个新的物理块，将旧块的数据复制进去。
3.  **更新指针**: 将 Inode 中的块指针指向新块。
4.  **递减引用**: 旧块的引用计数减 1（旧块现在只属于快照）。
5.  **写入**: 在新块上执行写入操作。

#### B. 目录修改 (Directory Modification) - 递归隔离策略
在 `DirectoryOps` (`addEntry`, `removeEntry`) 中，为了隔离快照和当前系统的元数据，我们采用了**深拷贝 (Deep Copy)** 策略：

1.  **目录块 CoW**: 如果目录的数据块是共享的，首先执行上述的块复制。
2.  **Inode 隔离**: 当目录块被复制时，系统会遍历该目录下的所有目录项 (Directory Entries)。
3.  **递归复制**: 对于目录中的每个文件，分配新的 Inode，复制原 Inode 的内容，并增加其底层数据块的引用计数。
    *   *结果*: 快照和当前系统拥有独立的 Inode 体系，但共享底层文件数据块。这确保了修改文件元数据（如大小、时间）不会影响快照。

## 4. 支持的操作

| 操作类型 | 描述 | CoW 行为 |
| :--- | :--- | :--- |
| `createSnapshot` | 创建快照 | 复制根 Inode，建立共享关系。 |
| `writeFile` | 修改文件内容 | 触发数据块的 CoW，确保快照读取到旧数据。 |
| `mkdir` / `touch` | 创建新文件/目录 | 触发父目录的 CoW (如果父目录被共享)。 |
| `rm` / `rmdir` | 删除 | 触发父目录 CoW；递减被删除文件数据块的引用计数（数据块可能因快照引用而保留）。 |

## 5. 集成与 API

快照功能主要通过 `SnapshotManager` 暴露，并深度集成到底层操作中：

*   **`SnapshotManager::createSnapshot()`**: 用户接口。
*   **`BlockManager::copyOnWrite(blockId)`**: 核心工具函数，处理块的复制与引用更新。
*   **`DirectoryOps::copyDirectoryBlock`**: 处理目录结构的深拷贝隔离。

## 6. 测试与验证

我们提供了专门的测试程序来验证快照的隔离性。

*   **运行测试**: `./test_snapshot`
*   **测试场景**:
    1.  **基准状态**: 创建文件 `p1.txt`，写入 "version1"。
    2.  **创建快照**: 生成快照 S1。
    3.  **修改文件**: 向 `p1.txt` 写入 "version2"。
    4.  **验证隔离**:
        *   读取当前 `p1.txt` -> 应为 "version2"。
        *   通过快照 S1 读取 `p1.txt` -> 应仍为 "version1"。
