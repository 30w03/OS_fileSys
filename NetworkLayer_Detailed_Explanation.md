# Peer Review System - 网络层深度解析

本文档旨在详细阐述 Peer Review System 网络层的设计与实现细节，帮助开发者全面理解系统是如何处理客户端与服务器之间的通信的。

## 1. 网络层概览

网络层是连接客户端用户界面与服务器后端逻辑的桥梁。本系统采用 **TCP/IP** 协议作为传输基础，并在此之上构建了一套自定义的应用层协议（PRSF），以确保数据传输的可靠性、有序性和完整性。

**核心职责：**
*   **连接管理**：建立、维护和断开网络连接。
*   **消息封装**：将业务数据封装成标准的消息格式。
*   **数据传输**：负责底层的字节流发送与接收。
*   **并发处理**：服务器端支持多用户同时在线操作。

---

## 2. 核心组件架构

网络层主要由三个核心类构成，它们分工明确，共同支撑起整个通信链路。

### 2.1 Connection 类 (连接基石)
*   **定位**：网络通信的最底层封装，不关心业务逻辑，只关心字节的收发。
*   **职责**：
    *   封装系统 Socket API。
    *   处理网络字节序（Big Endian）与主机字节序（Little Endian）的转换。
    *   提供 `sendMessage` 和 `receiveMessage` 接口，确保消息的完整发送与接收（处理 TCP 粘包/拆包问题）。
*   **关键实现**：`sendAll()` 和 `receiveAll()` 循环调用系统 `send/recv`，直到指定长度的数据全部传输完毕。

### 2.2 Client 类 (客户端代理)
*   **定位**：客户端应用程序的网络代理。
*   **职责**：
    *   维护与服务器的 `Connection` 对象。
    *   将用户的操作（如登录、提交论文）转换为具体的网络请求消息。
    *   等待并解析服务器的响应，将结果返回给 UI 层。
*   **业务映射**：
    *   `login()` -> 发送 `MSG_LOGIN_REQUEST`
    *   `submitPaper()` -> 发送 `MSG_SUBMIT_PAPER_REQUEST`
    *   `viewMyPapers()` -> 发送 `MSG_GET_MY_PAPERS_REQUEST`

### 2.3 Server 类 (服务端枢纽)
*   **定位**：服务器端的总控中心。
*   **职责**：
    *   监听特定端口，等待客户端连接。
    *   **并发模型**：采用 "Thread-per-Client" 模式。每当有一个新客户端连接，Server 就会创建一个新的线程 (`handleClient`) 专门服务该客户端，直到连接断开。
    *   消息分发：根据接收到的消息类型（Type），调用相应的业务处理函数（如 `handleLogin`, `handleSubmitPaper`）。

---

## 3. 自定义通信协议 (PRSF)

为了保证通信的规范性，系统定义了 **PRSF (Peer Review System Format)** 协议。

### 3.1 消息结构
每个消息都由 **固定长度的头部 (Header)** 和 **可变长度的载荷 (Payload)** 组成。

| 字段 | 长度 | 说明 |
| :--- | :--- | :--- |
| **Magic Number** | 4字节 | 固定为 `0x50525346` ("PRSF")，用于识别非法连接。 |
| **Version** | 1字节 | 协议版本号，当前为 1。 |
| **Type** | 1字节 | 消息类型标识（如 0x01=登录请求, 0x02=登录响应）。 |
| **Reserved** | 2字节 | 保留字段，目前填 0。 |
| **Length** | 4字节 | 后续 Payload 的字节长度。 |
| **Timestamp** | 8字节 | 消息发送的时间戳。 |
| **Checksum** | 4字节 | 整个消息的 CRC32 校验和，用于检测数据损坏。 |
| **Payload** | N字节 | 具体的业务数据（如用户名、密码、论文内容等）。 |

### 3.2 序列化机制
*   **字节序**：网络传输强制使用 **Big Endian**。发送前需将本地数据转换（`htonl`, `htons`），接收后需转换回本地字节序（`ntohl`, `ntohs`）。
*   **数据类型处理**：
    *   **整数**：直接转换字节序。
    *   **字符串**：先写入 4 字节长度，再写入字符串内容的字节流。
    *   **布尔值**：作为 1 字节传输。

---

## 4. 数据交互全流程解析

以 **"用户查看我的论文 (viewMyPapers)"** 为例，展示一次完整的请求-响应闭环：

### 第一阶段：客户端发送请求
1.  **构建消息**：`Client` 调用协议层方法，创建一个 `MSG_GET_MY_PAPERS_REQUEST` 类型的消息，Payload 中包含当前用户的 ID。
2.  **序列化**：将消息头部和 Payload 中的数据转换为网络字节序。
3.  **计算校验**：对整个数据包计算 CRC32 值并填入头部。
4.  **发送**：`Connection` 通过 Socket 将二进制流发送给服务器。

### 第二阶段：服务器处理
1.  **接收头部**：服务器线程先读取固定的头部长度。
2.  **协议检查**：验证 Magic Number 是否正确，版本是否兼容。
3.  **接收载荷**：根据头部中的 `Length` 字段，继续读取剩余的 Payload 数据。
4.  **完整性校验**：重新计算 CRC32，与头部中的 Checksum 比对。如果不一致，丢弃消息。
5.  **路由分发**：识别到类型为 `MSG_GET_MY_PAPERS_REQUEST`，调用 `handleGetMyPapers`。
6.  **业务逻辑**：
    *   查询数据库/文件系统，找到该作者的所有论文。
    *   查询每篇论文关联的评审记录。
    *   将这些对象（Paper, Reviews）打包成列表。

### 第三阶段：服务器发送响应
1.  **构建响应**：创建一个 `MSG_GET_MY_PAPERS_RESPONSE` 消息。
2.  **复杂对象序列化**：
    *   先写入论文数量。
    *   循环写入每篇论文的信息（ID, 标题等）。
    *   在每篇论文后，写入其评审数量，再循环写入评审详情（评分, 评语）。
3.  **发送**：通过 Socket 发回客户端。

### 第四阶段：客户端接收展示
1.  **接收与解析**：客户端接收二进制流，反序列化为内存中的对象列表（`List<PaperWithReviews>`）。
2.  **UI 渲染**：将对象数据传递给界面层进行展示。

---

## 5. 关键技术点总结

1.  **健壮性设计**：
    *   **Magic Number** 防止非本系统客户端误连。
    *   **CRC32 校验** 防止网络噪点导致的数据错乱。
    *   **全量读写** (`sendAll`/`receiveAll`) 解决 TCP 流式传输的不确定性。

2.  **跨平台兼容**：
    *   严格的字节序转换机制，确保在不同 CPU 架构（x86, ARM 等）的机器间通信正常。

3.  **模块解耦**：
    *   网络层只负责搬运数据，具体的业务逻辑（如怎么存文件、怎么校验密码）委托给 User/Review/Storage 模块处理。

## 6. 现有优化空间
*   **并发模型**：当前 Thread-per-Client 在高并发下资源消耗大，未来可升级为 IO 多路复用 (epoll/IOCP) 或线程池模型。
*   **安全性**：当前为明文传输，建议增加 SSL/TLS 加密层。
*   **性能**：对于大文件传输（如论文 PDF），可引入压缩算法或分块传输机制。
